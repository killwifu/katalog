-- +goose Up
-- Размер деривативов учитывается в квоте магазина при обработке фото,
-- но нигде не сохранялся — при удалении возвращался только оригинал,
-- и storage_used полз вверх. Значение для уже обработанных фото
-- восстановить нечем, поэтому 0: квота таких магазинов останется
-- завышенной, пока фото не пересоздадут.
ALTER TABLE photos ADD COLUMN drv_size bigint NOT NULL DEFAULT 0;

-- +goose Down
ALTER TABLE photos DROP COLUMN drv_size;
